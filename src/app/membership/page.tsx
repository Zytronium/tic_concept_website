export default function MembershipPage() {
    return (
        <div className="flex flex-col flex-1 items-center justify-center text-white section-container">
            <main className="flex flex-1 w-full flex-col items-center justify-between py-8 md:py-16 px-4 md:px-8">
                {/* Membership section */}
                {/* Include membership levels, and in phase 2, join membership widgets for each level */}
                <p className="text-lg md:text-xl text-center md:text-left">[List membership levels here]</p>
            </main>
        </div>
    );
}
